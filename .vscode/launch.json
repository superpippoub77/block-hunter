{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Block Hunter in Chrome",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8000/block-hunter-phaser.html",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start HTTP Server",
      "userDataDir": "${workspaceFolder}/.vscode/chrome-user-data"
    }
  ]
}
